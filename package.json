{
  "name": "undone-tools",
  "version": "1.0.0",
  "private": true,
  "description": "Undone Studio, Standards, and the isolated Chaos Vault parts and affective-pattern library.",
  "scripts": {
    "serve": "http-server . -p 5000 -c-1",
    "check": "node --check assets/products.js && node --check assets/analytics.js && node --check assets/app.js && node --check chaos-vault/assets/cv.js && node --check chaos-vault/library/affective-contracts.js",
    "validate": "node scripts/validate.mjs && node scripts/validate-offbeat.mjs",
    "test": "npm run check && npm run validate"
  },
  "keywords": [
    "affective-interface-engineering",
    "design-patterns",
    "static-site"
  ],
  "author": "Amy Do",
  "license": "ISC",
  "dependencies": {
    "http-server": "^14.1.1",
    "serve": "^14.2.5"
  }
}
